"""UI-слой Tag Manager: рендер вкладок и сайдбара.

Каждый модуль отвечает за одну вкладку (`render_*_tab`) либо за сайдбар. Логика
приложения живёт в `core/`, здесь только Streamlit-виджеты и оркестрация.

ВАЖНО (Streamlit-специфика): общие ресурсы (`worker`, `logger`) и состояние
(`st.session_state`) НЕ импортируются на уровне модуля — они читаются ВНУТРИ
функций рендера через `ui.context`. Так избегаем цикла `app ↔ ui` и обращения к
session_state до его инициализации в `app.py`.
"""
